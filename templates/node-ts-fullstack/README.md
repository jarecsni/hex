# {{ app_name }}

A fullstack scaffold. The `api/` subdirectory was filled by whichever
`kind: api` component the source roots resolved — swap by reconfiguring
the source root, not by editing this recipe.
